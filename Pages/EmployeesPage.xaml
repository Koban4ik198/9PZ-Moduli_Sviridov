<Page x:Class="pz6.Pages.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pz6.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="900"
      Title="EmployeesPage" Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
       
        
        <TextBlock Grid.Row="0" Text="Список сотрудников" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="10"/>

        <!-- Панель поиска и фильтрации -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            
            <TextBlock Text="Поиск по ФИО:" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="txtSearch" Width="150" Margin="5" 
                     TextChanged="txtSearch_TextChanged"/>
            
            <TextBlock Text="Фильтр по должности:" VerticalAlignment="Center" Margin="5"/>
            <ComboBox x:Name="cmbFilter" Width="150" Margin="5"
                      SelectionChanged="cmbFilter_SelectionChanged"/>
            
            <Button x:Name="btnAddEmployee" Content="Добавить сотрудника" 
                    Margin="5" Width="Auto" Click="btnAddEmployee_Click"/>
            
            <Button x:Name="btnRefresh" Content="Обновить" 
                Margin="5" Width="Auto" Click="btnRefresh_Click"/>
        </StackPanel>

        <!-- ListView для сотрудников -->
        <ListView x:Name="LViewEmployees" Grid.Row="2" Margin="10"
                  MouseDoubleClick="LViewEmployees_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding EmployeeID}" Width="50"/>
                    <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding LastName}" Width="120"/>
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FirstName}" Width="120"/>
                    <GridViewColumn Header="Отчество" DisplayMemberBinding="{Binding MiddleName}" Width="120"/>
                    <GridViewColumn Header="Должность" DisplayMemberBinding="{Binding Positions.PositionName}" Width="150"/>
                    <GridViewColumn Header="Телефон" DisplayMemberBinding="{Binding ContactPhone}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>

</Page>
