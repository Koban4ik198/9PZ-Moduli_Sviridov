<Page x:Class="pz6.Pages.AddEditEmployeePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pz6.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="900"
      Title="AddEditEmployeePage" Background="White">

    <ScrollViewer>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" Text="Данные сотрудника" 
                       FontSize="18" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <!-- Основные поля -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин (Email):" Margin="5"/>
                <TextBox x:Name="txtEmail" Grid.Row="0" Grid.Column="1" Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" Margin="5"/>
                <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Фамилия:" Margin="5"/>
                <TextBox x:Name="txtLastName" Grid.Row="2" Grid.Column="1" Margin="5" 
                         Text="{Binding LastName}"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Имя:" Margin="5"/>
                <TextBox x:Name="txtFirstName" Grid.Row="3" Grid.Column="1" Margin="5" 
                         Text="{Binding FirstName}"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Отчество:" Margin="5"/>
                <TextBox x:Name="txtMiddleName" Grid.Row="4" Grid.Column="1" Margin="5" 
                         Text="{Binding MiddleName}"/>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Телефон:" Margin="5"/>
                <TextBox x:Name="txtContactPhone" Grid.Row="5" Grid.Column="1" Margin="5" 
                         Text="{Binding ContactPhone}"/>

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Должность:" Margin="5"/>
                <ComboBox x:Name="cmbPosition" Grid.Row="6" Grid.Column="1" Margin="5"
                          DisplayMemberPath="PositionName"
                          SelectedValuePath="PositionID"
                          SelectedValue="{Binding PositionID, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

            <!-- Кнопки -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" 
                        HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button x:Name="btnSave" Content="Сохранить" Width="120" Margin="10"
                        Click="btnSave_Click"/>
                <Button x:Name="btnDelete" Content="Удалить" Width="120" Margin="10"
                        Click="btnDelete_Click"/>
                <Button x:Name="btnCancel" Content="Отмена" Width="120" Margin="10"
                        Click="btnCancel_Click"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
